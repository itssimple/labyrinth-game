// TODO(items round): data-driven item definitions. API in docs/CONTRACTS.md
// ("Items, combat & auras"). The ecs never imports this package — definitions
// are injected into createSimulation so mods can swap them.
export {};
