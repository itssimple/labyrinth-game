// TODO(phase 2): React shell + PixiJS renderer. Spec in docs/CONTRACTS.md.
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(<p>Labyrinth Game — not implemented yet</p>);
